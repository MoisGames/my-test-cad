import styled from "styled-components";

const ContactsPage = () => {
    const Main = styled.main `
        
    `
    return (
        <Main>
            CONTACTS
        </Main>
    );
};

export default ContactsPage;