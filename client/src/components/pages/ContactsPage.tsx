import styled from "styled-components";

const Main = styled.main `
        
`

const ContactsPage = () => {

    return (
        <Main>
            CONTACTS
        </Main>
    );
};

export default ContactsPage;